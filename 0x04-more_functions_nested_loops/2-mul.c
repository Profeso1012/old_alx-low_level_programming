#include "main.h"

/**
 * mul - prints the product of two numbers
 * @a: first number
 * @b: second number
 * Result: gives the product mul
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
